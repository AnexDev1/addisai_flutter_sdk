name: addis_ai_sdk
license: MIT
description: Production-ready Flutter and Dart SDK for Addis AI chat, voice, speech-to-text, and translation APIs.
version: 0.2.0
homepage: https://platform.addisassistant.com
repository: https://github.com/AnexDev1/addisai_flutter_sdk
issue_tracker: https://github.com/AnexDev1/addisai_flutter_sdk/issues

environment:
  sdk: ^3.2.2
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  mime: ^2.0.0
  http_parser: ^4.1.2
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  mockito: ^5.4.4
  build_runner: ^2.4.0
